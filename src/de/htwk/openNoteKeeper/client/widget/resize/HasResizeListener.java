package de.htwk.openNoteKeeper.client.widget.resize;

public interface HasResizeListener {

	/**
	 * Interface function to add a listener to this event
	 * 
	 * @param listener
	 */
	public void addResizedListener(ResizeListener listener);
}
